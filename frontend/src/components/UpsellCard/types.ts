export interface Props {
  // Conteúdo de marketing — defaults genéricos de "upgrade para o Pro".
  title?: string
  description?: string
  // Bullets opcionais (ex.: lista do que fica incluso no Pro) — sem default.
  highlights?: string[]
}
